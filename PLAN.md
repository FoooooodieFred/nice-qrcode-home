# nice-qrcode-home · 改进后的实施计划

> 更新：2026-09-19 · 核心 v1 已实现，进入验收。
> 原始方案保留在 [docs/PLAN.original.md](docs/PLAN.original.md)。

## 产品目标

一个无需账号的个人网址与二维码收藏空间。静态前端、IndexedDB 本地存储、JSON 可携带备份；中文优先，黑白界面配少量可定制强调色。用分组、搜索、星标和二维码分享，让收藏容易找、容易用。

不隐式填充用户数据。首次进入为空状态，可主动加载示例。浏览器、来源域名和设备之间不会自动同步数据。

## 本次改进

| 原方案问题                                                | 实施调整                                                                                         |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 所有功能同等优先，缺少验收条件                            | 先做完整的收藏闭环；按下表验收核心功能，单独列出可选后续工作                                     |
| 声称某个引擎“最优”“更鲁棒”，将 qr-scanner 固定描述为 WASM | 删除无实测依据的性能结论；以原生能力检测、库回退、真实失败保留原图为准                           |
| 用圆形特征猜测小程序码                                    | 不对平台作不可靠猜测；解码失败明确提示“可能是小程序码或普通图片”，由用户标注                     |
| 第三方图标与 reader 服务默认兜底                          | 默认不请求第三方；网站图标、直连元数据单独开启；不内置公共代理发送收藏链接                       |
| 备份格式、失败恢复不明确                                  | 版本化 schema、体积限制、ID/引用校验、恢复预览、单个事务替换，失败不破坏原数据                   |
| 多图导入可能产生部分写入                                  | 先逐个解析和表单确认，再原子保存有效批次；重复内容跳过并报告                                     |
| 链接内容可直接打开                                        | 仅 HTTP(S)、weixin、alipays 协议可跳转；其他内容仅作为文本保存/生成 QR                           |
| 扩展点只有接口名称                                        | 提供可工作的 adapter、decoder、metadata、QR renderer、card action 与设置注册接口及示例           |
| 多套 UI/动画库叠加                                        | React 18 + 原生 dialog/表单 + CSS tokens；移除不必要的 Tailwind/shadcn/motion 依赖，保留 dnd-kit |
| 可访问性与资源生命周期缺失                                | dialog 焦点限制/Escape、按钮名称、键盘排序、减少动效；释放摄像头和对象 URL                       |

## 技术实现

- React 18、TypeScript、Vite；CSS variables 驱动主题。
- Zustand 保存当前数据库快照和 UI 所需状态，Dexie 是唯一持久化来源，不向 localStorage 复制图片。
- Zod 验证备份、设置和收藏数据；Vitest + fake-indexeddb 覆盖数据层和失败路径。
- qr-scanner、jsQR 和 qr-code-styling 动态加载。
- dnd-kit 提供组内鼠标/键盘排序，跨组移动通过编辑表单。
- Lucide 图标；不加载外部字体，不需要 API key 或服务器。

## 已实现的核心工作包与验收

| 工作包        | 实现内容                                                                                             | 验收条件                                                                   |
| ------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| M1 基础       | 项目配置、数据 schema、Dexie adapter、Zustand、CSS tokens                                            | 构建成功；刷新后收藏/设置保留；存储不可用有错误与重试                      |
| M2 导入       | 多行网址/文本、多图上传、全页拖放、剪贴板图片、摄像头入口、逐项审核、重复跳过                        | 不识别的图片仍可保存；无效文件明确报错；摄像头关闭时销毁；批次保存为事务   |
| M3 收藏       | 分组新增/改名/排序/删除、未分组回收、卡片编辑/删除、星标、搜索、类型/分组过滤、组内拖动              | 删除分组不删除收藏；搜索覆盖标题/描述/网址/标签/笔记；排序可持久化         |
| M4 QR 与分享  | 原图或生成 QR、三种样式、PNG/SVG 下载、PNG 复制、系统分享、分享卡 PNG                                | 原图不假造为 SVG；二维码生成失败提示；剪贴板/分享受浏览器能力控制          |
| M5 设置与扩展 | 浅/深/系统主题、强调色、三种背景、2–5 列、密度、圆角、名称、QR 样式、元数据/图标开关、备份/恢复/清空 | 导入先校验与预览，明确替换后才恢复；原图跟随 JSON 往返；扩展设置持久化     |
| M6 交付       | 中文/英文 README、架构接口文档、MIT、单元测试、构建产物、浏览器走查                                  | npm test / npm run build 通过；关键手动验收记录保存在 docs/verification.md |

## 资源、数据与隐私边界

- 单次最多 30 项；图片格式 PNG/JPEG/WebP/GIF，单图 10 MB、2400 万像素上限。
- 备份文件最多 60 MB；最多 10000 收藏、200 分组、1000 图片，图片仅允许 raster data URL。
- 解码顺序：可用的原生 BarcodeDetector → qr-scanner → jsQR 多尺寸/反色重试。每层失败不阻止下一层。
- 私有小程序码不能靠标准 QR 引擎保证解析，原始图片永远是保底资产。
- 自动元数据使用目标站点的 CORS 许可，4 秒超时，无结果仍可手填。
- 数据只存于当前 origin；清除网站数据、使用隐私模式、切换部署域名都可能导致数据不可用，定期备份。
- 相机通常需要 HTTPS 或 localhost、浏览器权限和真实设备；复制图片/系统分享同样有浏览器限制。
- 注册的扩展是受信任源码，不支持远程脚本或在浏览器中粘贴任意可执行插件。

## 独立后续范围（不作为 v1 已完成功能）

- PWA/service worker 与安装体验；离线首次打开不保证。
- 云端/WebDAV 同步、加密、冲突合并。
- 完整英文界面：目前集中顶层中文文案，尚未迁移所有弹窗文案；README 含英文。
- 完整 Ctrl+K 命令菜单：v1 Ctrl/Cmd+K 聚焦搜索，Ctrl/Cmd+, 打开设置。
- 自定义卡片图标上传、每张卡片独立颜色、二维码渐变/logo/模板市场。
- 多码同图分割识别、圆形图案分类器、跨组拖动。
- GitHub Pages 自动发布 workflow；本地构建已经兼容子目录部署，实际发布需指定仓库/平台。

## 使用与验收

见 [README.md](README.md)、[docs/architecture.md](docs/architecture.md) 和 [docs/verification.md](docs/verification.md)。

参考接口：
[qr-scanner](https://github.com/nimiq/qr-scanner) ·
[jsQR](https://github.com/cozmo/jsQR) ·
[qr-code-styling](https://github.com/kozakdenys/qr-code-styling)。
